{
  "permissions": {
    "defaultMode": "acceptEdits",
    "allow": [
      "Bash(*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd c:/hansungbolt-overtime && git diff --quiet && git diff --cached --quiet || (git add -A -- ':!.claude/' && git diff --cached --quiet || (git commit -m 'auto: deploy update' && git push origin main)) 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Đang deploy lên Vercel..."
          }
        ]
      }
    ]
  }
}
